package com.agustin.tarati.ui.components.game.draw.pieces

enum class ConversionAnimationType {
    FROM_CENTER, // El cambio surge desde el centro
    FROM_BORDER, // El cambio surge desde el borde
    FLIP,        // La pieza voltea como una moneda, revelando el color convertido
}